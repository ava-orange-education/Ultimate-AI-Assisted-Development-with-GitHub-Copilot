// Prompt Used: Create a TypeScript object for a user with properties: name, age, and isAdmin, and access those properties

const user = {
    name: "Alice",
    age: 28,
    isAdmin: true,
  };
  
  console.log(`Name: ${user.name}`);
  console.log(`Age: ${user.age}`);
  console.log(`Is Admin: ${user.isAdmin}`);
  