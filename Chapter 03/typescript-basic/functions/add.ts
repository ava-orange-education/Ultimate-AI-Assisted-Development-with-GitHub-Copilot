// Prompt Used: Write a function in TypeScript that adds two numbers and returns a number

function addNumbers(a: number, b: number): number {
    return a + b;
  }
  
  console.log(addNumbers(5, 10)); // Output: 15
  